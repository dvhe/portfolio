// import { useState } from "react";

// global.window as any

// export const srcBackground = `https://source.unsplash.com/hDEPZRu0nn0/${window.innerWidth}x${window.innerHeight}`;
// export const background = useState<string>(srcBackground);
// export const [clicked, wasClicked] = useState<boolean>(false);
